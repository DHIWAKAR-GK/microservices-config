server:
  port: 9000

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3306/api_gateway_learn
    username: root
    password: Dhiwakar@2003
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
